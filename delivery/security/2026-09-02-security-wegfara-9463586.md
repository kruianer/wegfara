---
type: security
repo: Wegfara
commit: 9463586
date: 2026-09-02
---

# Security: Wegfara (9463586)

Automatisch erstellt vom appbaua-Worker am 2026-09-02.

Alle Prüfungen sind abgeschlossen, das Repo ist unverändert (`git status` leer, HEAD `9463586`). Ich habe Findings — hier der Bericht.

---
project: wegfara
commit: 9463586
date: 2026-09-02
---

# Security-Check — 2026-09-02

## Kurz-Zusammenfassung

`delivery/security.md` lag vor und war das SOLL; ergänzend herangezogen wurden `CLAUDE.md`, `delivery/stack.md`, `delivery/devops.md` und die fünf Vorberichte (2026-07-29 bis 08-26). Alle Prüfungen waren lesend; live abgefragt wurde ausschließlich über die öffentlichen URLs.

**Seit dem letzten Bericht (`40a680b`) gab es keine einzige Code-Änderung.** Der Diff umfasst drei Commits, die ausschließlich Dokumente hinzufügen: eine Idee, einen Code-Review und den Security-Bericht selbst — 497 Zeilen, alle unter `delivery/`. Kein Byte in `app/`, `lib/`, `middleware.ts`, `next.config.ts`, `deploy/` oder `package.json`. Folgerichtig bestehen **alle acht Befunde des Vorberichts unverändert fort**; ich habe jeden davon neu geprüft statt ihn zu übernehmen.

Das hoch eingestufte Transport-Finding ist **erneut live bestätigt**: `dev.wegfara.com` liefert die Anwendung weiterhin über unverschlüsseltes `http://` mit Status 200 und ohne jede Weiterleitung aus, es fehlt jeder `Strict-Transport-Security`-Header, und in diesem Fall setzt die Anwendung das Sitzungs-Cookie ohne `Secure`-Flag — direkt gegenübergestellt, gemessen am 02.09.2026:

```
http : set-cookie: wegfara_sitzung=; Path=/; Max-Age=0; HttpOnly; SameSite=lax
https: set-cookie: wegfara_sitzung=; Path=/; Max-Age=0; Secure; HttpOnly; SameSite=lax
```

Zwei Befunde stufe ich gegenüber dem Vorbericht **herauf**. Das fehlende Backup ist nun der **fünfte Bericht in Folge** mit identischem Befund, während der Backlog (`draft`, `ready`, `in-progress`) weiterhin nur `.gitkeep` enthält und die Anwendung inzwischen echte Anmeldedaten, Passkeys und Reisedaten führt — es ist der einzige Befund, bei dem ein Vorfall nicht reparabel wäre, deshalb **hoch**. Und die prod-Umgebung antwortet zum **vierten Mal in Folge** mit HTTP 530; sie wurde damit **noch nie live sicherheitsgeprüft**, was ich als eigenständige Prüflücke (mittel) ausweise statt wie bisher als Randnotiz.

Positiv, alles neu verifiziert: Die Anmeldung greift zuverlässig — `/go`, `/plan`, `/konto` leiten unangemeldet mit 307 auf `/anmeldung` um; `trips`, `poi-search`, `place-search`, `poi-status`, `search-area` und `activity-option-selection` antworten mit `401 {"error":"nicht angemeldet"}`. Kein Secret im Arbeitsbaum, im Git-Index oder in der Git-Historie. Keine SQL-Injektion im Datenzugriffs-Layer.

**Nicht prüfbar in dieser Sitzung:** Kein SSH-Zugang zum Beelink hinterlegt (`~/.ssh` existiert nicht). Infrastruktur, Tunnel-Konfiguration, tatsächlicher Backup-Bestand und Dateirechte der env-Dateien konnten deshalb nur aus Code und Config beurteilt werden.

## Befunde

### 1. Anwendung über unverschlüsseltes HTTP erreichbar, Sitzungs-Cookie dann ohne `Secure` (hoch)

**Live verifiziert (02.09.2026)** — unverändert seit dem 19.08.:

- `http://dev.wegfara.com/` → **200** mit vollständigem App-HTML, kein `Location`-Header, keine Weiterleitung auf `https://`.
- `http://dev.wegfara.com/anmeldung` → **200**, die Anmeldeseite ist im Klartext benutzbar.
- Weder die http- noch die https-Antwort trägt `Strict-Transport-Security`. Der Browser hat keinen Anlass, von sich aus auf HTTPS zu wechseln.
- `POST http://dev.wegfara.com/api/auth/abmelden` liefert das Sitzungs-Cookie **ohne** `Secure`, dieselbe Anfrage über HTTPS **mit** (Gegenüberstellung siehe Zusammenfassung).

Ursache im Code: `lib/auth/cookies.ts:36` leitet das `Secure`-Flag aus `connectionIsSecure(x-forwarded-proto, url)` ab, aufgerufen in `middleware.ts:38`. Der Fallback ohne `Secure` ist laut Kommentar in `cookies.ts:26-31` für die lokale Entwicklung gedacht, greift hinter dem Cloudflare Tunnel aber genauso, sobald jemand `http://` aufruft — die Anwendung sieht dort ohnehin eine unverschlüsselte Verbindung und richtet sich allein nach dem von Cloudflare gesetzten Kopf.

Wer sich über `http://` per Anmeldelink oder Notfallcode anmeldet, bekommt ein Sitzungs-Token, das jeder im selben Hotel- oder Café-WLAN mitlesen und — wegen der 90-Tage-Sitzung aus Finding 4 — drei Monate lang weiterverwenden kann, ohne Möglichkeit des Widerrufs. Das ist genau das Szenario, für das `delivery/security.md` den Begleiter im Mobilnetz ausdrücklich vorsieht.

`delivery/security.md` fordert: *„HTTPS: Pflicht fuer beide Umgebungen … Kein unverschluesselter Zugriff, keine Zertifikatswarnung."* **Nicht erfüllt.**

**Empfehlung** — dreifach absichern, in dieser Reihenfolge:
1. In Cloudflare für beide Zonen **„Always Use HTTPS"** einschalten und **HSTS** aktivieren (`max-age` ≥ 6 Monate, `includeSubDomains`). Das ist der eigentliche Fix, kostet keine Codeänderung und wirkt sofort für beide Umgebungen.
2. Als zweite Absicherung in `middleware.ts`: bei `x-forwarded-proto !== "https"` per 308 auf `appUrl()` + Pfad umleiten.
3. `sessionCookieOptions()` darf im Betrieb nie ohne `Secure` ausliefern — `secure: process.env.NODE_ENV === "production" ? true : connectionIsSecure(...)`. Der Dev-Komfort bleibt, das Produktionsrisiko verschwindet.

### 2. Backup weiterhin nicht vorhanden — fünfter Bericht in Folge, weiterhin kein Backlog-Eintrag (hoch, heraufgestuft)

Aus Code und Config erschlossen; ein Live-Blick auf `~/wegfara-backups/` war mangels SSH-Zugang nicht möglich.

`delivery/security.md` fordert ein tägliches, automatisches Backup von DB **und** Bilddateien in einem gemeinsamen Lauf, ein Ziel **außerhalb** des Beelink und eine **getestete** Wiederherstellung. Der Stand: eine Volltextsuche über `lib/`, `app/`, `scripts/` und `migrations/` nach `backup|sicherung|restore|pg_dump` findet als einzigen Treffer `lib/mail/smtp-mailer.test.ts` — also keinerlei Implementierung.

Die einzige Sicherung bleibt der Schritt „Backup vor dem Deploy" in `.github/workflows/deploy-prod.yml:31-40`. Er legt `db.sql` und `images.tar.gz` unter `$HOME/wegfara-backups/pre-deploy_$TS` ab — auf **derselben Maschine** — und läuft nur bei einem manuell ausgelösten prod-Deploy, nicht täglich. Nach der eigenen Vorgabe (*„Ein Backup, das nur auf derselben Maschine liegt, gilt als nicht vorhanden"*) ist damit **kein Backup vorhanden**. Ein getesteter Restore ist nirgends dokumentiert.

Ich stufe das von mittel auf **hoch** herauf. Nicht weil sich technisch etwas verschlechtert hätte, sondern wegen der Kombination aus drei Umständen: es ist der fünfte identische Befund in Folge; `delivery/requirements/{draft,ready,in-progress}/` enthalten weiterhin ausschließlich `.gitkeep`, während in `delivery/idea/` inzwischen vier Feature-Ideen liegen; und die Anwendung führt seit req-016 echte Anmeldedaten, Passkeys und Reisedaten. Alle anderen Befunde in diesem Bericht beschreiben Risiken, die sich nach einem Vorfall beheben lassen — dieser nicht.

**Empfehlung:** Ein Requirement „Backup und Wiederherstellung" formulieren und **vor** dem nächsten Feature einlasten: täglicher automatischer Lauf, DB + Bilder in einem zueinander passenden Stand, Übertragung an ein Ziel außerhalb des Beelink, dazu ein einmal durchgeführter und dokumentierter Restore-Test. Das ist der Befund, der die höchste Priorität verdient.

### 3. Drei schreibende Endpunkte prüfen die Anmeldung, aber nicht den Besitz (mittel)

Aus Code erschlossen; die 401-Antworten habe ich live bestätigt, die eigentliche Lücke aber **nicht live ausgelöst**, um keine echten Daten zu verändern.

Alle drei Routen verlangen inzwischen eine Sitzung, geben die vom Client gelieferte ID danach aber ungeprüft an den Datenzugriff weiter. Der Mandant aus `session.participant.accountId` wird in keiner der drei Routen auch nur gelesen — `currentSession()` wird nur auf Wahrheitswert geprüft:

| Route | Datenzugriff | Fehlt |
|---|---|---|
| `app/api/poi-status/route.ts:22` | `lib/db/pois.ts:55` — `update poi set status = $2 where id = $1` | kein `account_id`-Bezug |
| `app/api/search-area/route.ts:36,51` | `lib/db/search-area.ts:45,65` — `delete from search_area where trip_id = $1` | kein `account_id`-Bezug |
| `app/api/activity-option-selection/route.ts:22` | `lib/db/activity-option-selections.ts:44` — `insert … on conflict … do update` | kein `account_id`-Bezug; zusätzlich wird `selected_activity_id` nicht gegen dieselbe Reise geprüft |

`delivery/security.md` ist hier ausdrücklich: *„Auch solange nur ein Mandant existiert, ist ein fehlender Mandantenfilter ein Sicherheitsmangel — er faellt erst auf, wenn es zu spaet ist."* Genau diese Situation liegt vor: heute gibt es einen Account, aber `delivery/security.md` plant Familie und Freunde als Teilnehmer ein. Ab dem zweiten Teilnehmer kann jeder Angemeldete mit einer fremden POI- oder Reise-ID Bewertungsstatus, Suchgebiet und Options-Wahl einer fremden Reise überschreiben — `clearSearchArea` löscht dabei ohne Rückfrage.

Das richtige Muster liegt im selben Repo direkt daneben: `lib/db/trips.ts:55` prüft mit `belongsToAccount()`, und `app/api/trips/route.ts` gibt konsequent `404 unknown trip` zurück, wenn die Reise einem anderen Account gehört. `poi-search` erzwingt den Besitz implizit über `listSearchAreas(db, accountId)`.

**Empfehlung:** Die drei Datenzugriffs-Funktionen um `accountId` erweitern und den Filter in die SQL-Bedingung ziehen (z. B. `update poi set status = $3 from trip t where poi.id = $1 and t.id = poi.trip_id and t.account_id = $2`), Rückgabe `false` → `404` statt stillem `ok`, wenn nichts getroffen wurde. Bei der Options-Wahl zusätzlich prüfen, dass `selected_activity_id` zur selben Reise gehört. Je ein Test „fremde ID wird abgewiesen" pro Route — `trips.test.ts` liefert die Vorlage.

### 4. Sitzungen: feste 90 Tage statt Reisezeitraum, kein Fernwiderruf bei Geräteverlust (mittel)

Aus Code erschlossen.

- `lib/auth/lifetime.ts:12` setzt `SESSION_DURATION_MS = 90 * DAY_MS` und verlängert bei Nutzung (`shouldRenewSession`, ab einem verbrauchten Tag). Eine regelmäßig genutzte Sitzung läuft damit praktisch nie ab. `delivery/security.md` verlangt eine aus dem Reisezeitraum abgeleitete Dauer, die *„danach automatisch ablaeuft"*. Die Abweichung ist in `lifetime.ts:6-11` bewusst dokumentiert und für req-016 ausgeklammert — sie steht aber in keinem Backlog-Eintrag und läuft damit Gefahr, dauerhaft zu bleiben.
- `delivery/security.md` verlangt außerdem: *„Sitzungen lassen sich aus der Ferne beenden: bei Geraeteverlust kann der betroffene Teilnehmer — und der Organisator der Reise — alle Sitzungen des Kontos widerrufen."* In `lib/db/sessions.ts` existieren nur `deleteSessionByToken` (Zeile 89) und `deleteExpiredSessions` (Zeile 99) — kein Widerruf über alle Geräte. `app/konto/konto-view.tsx:217` sagt dem Nutzer wörtlich: *„Das Abmelden beendet die Sitzung sofort — auf diesem Gerät."* Ein verlorenes Smartphone behält bis zu 90 Tage Zugriff auf alle Reisedaten, ohne Möglichkeit, das zu unterbinden.
- Nebenbefund, unverändert: `deleteExpiredSessions` wird **außerhalb der Tests nirgends aufgerufen** (per Volltextsuche über das ganze Repo bestätigt — die einzigen Treffer sind `lib/db/sessions.ts:99` und `lib/db/sessions.test.ts`). Abgelaufene Sitzungszeilen bleiben unbegrenzt liegen.

Finding 1 verschärft diesen Punkt: ein über HTTP abgegriffenes Token ist wegen der 90 Tage kein kurzer Zwischenfall, sondern ein Quartal Vollzugriff ohne Abschaltmöglichkeit.

**Empfehlung:** Ein Folge-Requirement zu req-016: „Alle Sitzungen dieses Kontos beenden" auf `/konto` (`delete from session where participant_id = $1`, inklusive Wirkung auf die eigene laufende Sitzung), dazu der Aufruf von `deleteExpiredSessions` beim Start oder in einem täglichen Lauf. Die Bindung der Sitzungsdauer an den Reisezeitraum entweder einlasten oder die Abweichung in `delivery/security.md` bewusst festschreiben — der jetzige Schwebezustand ist die schlechteste der drei Möglichkeiten.

### 5. Vier High-Schwachstellen in Abhängigkeiten, unverändert seit fünf Wochen (mittel)

Aus `npm audit --omit=dev` verifiziert (lokal ausgeführt, 02.09.2026). Installiert: `next 16.2.12`, `sharp`, `postcss`, `nanoid`.

- `postcss` ≤ 8.5.22 — XSS über unescaptes `</style>` plus drei Path-Traversal-/Arbitrary-File-Read-Advisories über `sourceMappingURL` (GHSA-qx2v-qp2m-jg93, -6g55-p6wh-862q, -fxqj-rqcc-2cmp, -r28c-9q8g-f849)
- `sharp` < 0.35.0 — libvips-CVEs 2026-33327, -33328, -35590, -35591 (GHSA-f88m-g3jw-g9cj)
- `nanoid` < 3.3.18 — Endlosschleife bei Größe 0 (GHSA-2v37-7h3g-55p8)
- `next` 9.3.4-canary.0 – 16.3.0-preview.10 ist über `postcss` und `sharp` mitbetroffen

`npm audit` meldet für alle vier: *„fix available via `npm audit fix`"* — ohne `--force`, ohne Major-Wechsel. Betroffen ist überwiegend der Build-Weg, nicht direkt Nutzereingaben aus dem Internet; der Aufwand ist aber so gering, dass das kein Grund zum Warten ist. Seit dem 29.07. unverändert offen.

Ergänzend aus `npm outdated`: `next` 16.2.12 → 16.3.4, `pg` 8.22.0 → 8.23.0, `nodemailer` 9.0.5 → 9.1.1, `@simplewebauthn/server` 13.3.2 → 13.3.3, `maplibre-gl` 6.0.0 → 6.6.0 — alles innerhalb derselben Major-Version. `openai` 5.23.2 → 7.9.0 ist ein Major-Sprung und gehört nicht in dieses Update.

**Empfehlung:** `npm audit fix` einspielen, die genannten Minor-Updates mitnehmen, danach `npm test`, `npm run build` und `npx tsc --noEmit` grün ziehen und über den normalen dev-Deploy abnehmen.

### 6. prod-Umgebung antwortet zum vierten Mal in Folge mit HTTP 530 — nie live geprüft (mittel, heraufgestuft)

**Live verifiziert (02.09.2026):** `https://app.wegfara.com/` und `http://app.wegfara.com/` antworten beide mit **HTTP 530**. Der `server: cloudflare`-Header ist gesetzt, die Antwort kommt also von Cloudflare — 530 bedeutet, dass Cloudflare den Ursprung hinter dem Tunnel nicht erreicht. Die zurückgelieferten Header (`x-frame-options: SAMEORIGIN`, `referrer-policy: same-origin`) stammen von Cloudflares Fehlerseite, nicht von der Anwendung.

Das ist der vierte Bericht in Folge mit demselben Ergebnis (19.08., 26.08. und davor). Ich weise es diesmal als eigenständigen Befund aus statt als Randnotiz, weil daraus zwei Dinge folgen:

- **Betrieblich:** Läuft prod tatsächlich nicht, ist die produktive Umgebung seit mindestens vier Wochen ausgefallen. Läuft sie, ist der `cloudflared`-Tunnel oder der prod-Container defekt. Beides sollten Sie wissen.
- **Für die Sicherheitsprüfung:** Die prod-Umgebung wurde dadurch **noch nie live geprüft**. Alle Aussagen zu prod in diesem und allen Vorberichten sind ausschließlich aus Code und Config erschlossen. Insbesondere ist unbekannt, ob Finding 1 (HTTP ohne Weiterleitung, Cookie ohne `Secure`) auf prod ebenso greift — nach dem gemeinsamen Code in `lib/auth/cookies.ts` und der gemeinsamen Cloudflare-Konfiguration ist genau das zu erwarten, aber ungeprüft.

**Empfehlung:** Auf dem Beelink `docker compose -p wegfara-prod … ps` und die Logs des `cloudflared`-Containers prüfen (per `devops.md` darf das nur in einer Session mit Ihnen geschehen, nicht autonom). Sobald prod wieder antwortet, sollte der nächste Security-Check die Punkte 1, 7 und 9 gegen `app.wegfara.com` wiederholen.

### 7. Keine Sicherheits-Header gesetzt (niedrig)

**Live verifiziert (02.09.2026):** Die Antworten von `dev.wegfara.com` enthalten über http wie https weder `Content-Security-Policy` noch `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy` oder `Permissions-Policy`. `next.config.ts` definiert unverändert keine `headers()`-Sektion — die Datei enthält nur `output: "standalone"` und `outputFileTracingIncludes`.

Konkret relevant hier: ohne `frame-ancestors`/`X-Frame-Options` lässt sich die angemeldete Anwendung in einen fremden Rahmen einbetten — die schreibenden Aktionen in `/plan` sind per Klick auslösbar, und `SameSite=lax` schützt davor nicht, weil die Anfragen aus dem eingebetteten, gleichnamigen Ursprung kämen. In Kombination mit Finding 3 (kein Besitz-Nachweis auf drei schreibenden Routen) ist das mehr als eine Formalie. `Permissions-Policy` ist das passende Werkzeug für eine App, die laut `delivery/stack.md` Kamera und Standort nutzt.

**Empfehlung:** In `next.config.ts` eine `headers()`-Sektion für alle Pfade: `X-Frame-Options: DENY` bzw. CSP mit `frame-ancestors 'none'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy: geolocation=(self), camera=(self)`. Eine vollständige CSP kann später folgen — die vier einfachen Header sind ohne Risiko sofort setzbar.

### 8. `cloudflared:latest` ist nicht festgenagelt (niedrig)

Aus `deploy/docker-compose.yml:66` erschlossen: Während `postgres:17-alpine` und `node:22-alpine` gepinnt sind, zieht der Tunnel-Container `cloudflare/cloudflared:latest`. Jeder Deploy kann damit unbemerkt eine andere Version genau der Komponente hochziehen, die den einzigen Weg von außen in das Heimnetz aufbaut — und ein Rollback wird zum Ratespiel. Angesichts von Finding 6 (Tunnel erreicht prod nicht) ist die fehlende Versions-Nachvollziehbarkeit hier nicht rein theoretisch: bei einer Störung lässt sich nicht feststellen, ob sich die Tunnel-Version zwischenzeitlich geändert hat.

**Empfehlung:** Auf eine konkrete Version pinnen (z. B. `cloudflare/cloudflared:2026.8.0`) und beim Dependency-Update mitziehen.

### 9. `robots.txt`: Cloudflare stellt `Allow: /` vor die eigene `Disallow: /`-Regel (niedrig)

**Live verifiziert (02.09.2026).** Im Repo steht `public/robots.txt`:

```
# wegfara ist eine private Anwendung — nicht fuer Suchmaschinen.
User-agent: *
Disallow: /
```

Ausgeliefert wird beides hintereinander — Cloudflares „Managed content"-Block **zuerst**, die Datei der Anwendung danach:

```
# BEGIN Cloudflare Managed content
User-agent: *
Content-Signal: search=yes,ai-train=no,use=reference
Allow: /
User-agent: Amazonbot
Disallow: /
…
# END Cloudflare Managed Content
# wegfara ist eine private Anwendung — nicht fuer Suchmaschinen.
User-agent: *
Disallow: /
```

Gegenüber dem 26.08. hat sich das Bild leicht verändert: damals war die Datei der Anwendung gar nicht zu sehen, jetzt steht sie hinter dem Cloudflare-Block. Besser ist das nicht — es entstehen **zwei `User-agent: *`-Gruppen mit gegensätzlicher Aussage**. Crawler führen doppelte Gruppen zusammen, und bei gleich langem Pfadmuster gewinnt nach Googles Regel die *weniger* einschränkende: `Allow: /` setzt sich gegen `Disallow: /` durch. Die Datei sagt also weiterhin das Gegenteil dessen, was das Repo behauptet, nur jetzt auf eine Weise, die beim Draufschauen wie ein Widerspruch statt wie eine Überschreibung aussieht.

Das unmittelbare Risiko ist begrenzt, weil alles außer Startseite und Anmeldeseite hinter der Anmeldung liegt (live bestätigt) — indexierbar sind also nur zwei Seiten ohne Nutzerdaten. Bemerkenswert ist vor allem das Muster: **Cloudflare verändert hier eine Ausgabe der Anwendung.** Dieselbe Klasse von Managed-Funktionen greift auch bei Headern und Weiterleitungen, also genau dort, wo Finding 1 und 7 den Fix erwarten.

**Empfehlung:** In den Cloudflare-Einstellungen der Zone „Managed robots.txt" / Content Signals abschalten, wenn die eigene `Disallow: /`-Aussage gelten soll — oder die Datei im Repo entfernen und die Steuerung bewusst an Cloudflare übergeben. Beides ist vertretbar; der jetzige Zustand ist nur deshalb schlecht, weil zwei widersprüchliche Regeln ausgeliefert werden. Bei der Gelegenheit die Managed-Funktionen der Zone einmal insgesamt durchsehen — sie sind der Grund, warum ein Fix für Finding 1 in Cloudflare selbst gesetzt werden sollte und nicht nur im Code.

## Was in Ordnung war

Alles Folgende wurde in diesem Lauf neu geprüft, nicht aus dem Vorbericht übernommen.

- **Anmeldung greift, live bestätigt:** `/go`, `/plan`, `/konto` → 307 auf `/anmeldung?weiter=…`; `/api/trips`, `/api/poi-search`, `/api/place-search`, `/api/poi-status`, `/api/search-area`, `/api/activity-option-selection` → 401 `{"error":"nicht angemeldet"}`. `/api/health` ist bewusst offen und gibt nur `{"status":"ok"}` ohne DB-Zugriff preis.
- **Öffentliche Auth-Endpunkte prüfen selbst:** `/api/auth` ist im middleware-Matcher als öffentlicher Prefix eingetragen, die Routen dahinter schützen sich aber eigenständig — `notfallcodes` (GET und POST) und `passkey/registrierung` rufen jeweils `currentSession()` auf und antworten sonst mit 401. Der Prefix-Test in `isPublicPath()` ist korrekt geankert (`pathname === prefix || startsWith(prefix + "/")`), ein hypothetisches `/api/auth-admin` würde also nicht mitgefangen.
- **Geheimnisse:** Kein hartkodiertes Secret im Arbeitsbaum (Mustersuche nach `sk-…`, `AKIA…`, `BEGIN … PRIVATE KEY`, `eyJ…` und Zuweisungen an `password|passwort|secret|api_key|token` — kein Treffer), keine `.env`-Datei im Baum, keine im Git-Index, und **keine je in der Git-Historie angelegt** (`git log --all --diff-filter=A`). `.gitignore` deckt `.env`, `.env.local` und `.env*.local` ab. `DATABASE_URL`, `AUTH_SECRET`, `OPENAI_API_KEY`, `SMTP_*` und `CLOUDFLARE_TUNNEL_TOKEN` stehen nur als `${VAR}`-Referenzen in `deploy/docker-compose.yml`.
- **Umgang mit Geheimnissen im Code:** Sitzungs-Token, Anmeldelink-Token und Notfallcodes liegen nur als SHA-256-Prüfsumme in der DB; vom Passkey wird ausschließlich der öffentliche Schlüssel abgelegt (`lib/db/credentials.ts`), plus Zähler zum Erkennen geklonter Geräte. Notfallcodes werden genau einmal ausgeliefert und beim Abholen gelöscht.
- **Keine SQL-Injektion:** Der gesamte Datenzugriffs-Layer arbeitet mit `$1`-Platzhaltern. Die einzigen Template-Interpolationen in `lib/db/` sind `${COLUMNS}` in `credentials.ts:36,52` (eine feste Spaltenliste, kein Nutzereingang) sowie reine Datums-Formatierung in `sql-datetime.ts` und `trips.ts`.
- **Anmeldung im Detail:** Rate-Limits auf beiden offenen Endpunkten (`lib/auth/rate-limit.ts`, mit Aufräumen abgelaufener Schlüssel); einheitliche Antwort bei bekannter und unbekannter Adresse; Anmeldelink 15 min gültig, WebAuthn-Challenge 5 min.
- **Mandantentrennung auf den Lesepfaden:** `listTrips`, `listPois`, `listSearchAreas` filtern durchgängig nach `account_id`. `updateTrip`/`deleteTrip` prüfen den Besitz über `belongsToAccount()` und liefern `404 unknown trip` statt eines Hinweises auf fremde Daten — das Muster, das Finding 3 fehlt.
- **Erreichbarkeit der Infrastruktur:** PostgreSQL ohne Portfreigabe, nur im Compose-Netz; die App an `127.0.0.1:${APP_PORT}` gebunden; Zugang von außen ausschließlich über den Cloudflare Tunnel. Entspricht `delivery/security.md`.
- **Container:** Mehrstufiger Build, Laufzeit als unprivilegierter Nutzer `nextjs` (UID 1001), Bildverzeichnis als Host-Volume außerhalb des Containers, `postgres` und `node` auf konkrete Tags gepinnt.
- **Deploy:** `deploy-prod.yml` hat unverändert keinen Push- oder Merge-Trigger, nur `workflow_dispatch` mit Bestätigungswort `deploy`, das in einem eigenen ersten Schritt geprüft wird. `deploy-dev.yml` läuft auf Push nach `dev` — wie in `devops.md` festgelegt.
- **Externe Dienste & Datenschutz:** Der KI-Aufruf liegt hinter der austauschbaren Schnittstelle in `lib/ai/`, Modell über `OPENAI_MODEL` übersteuerbar. An OpenAI, Nominatim und Overpass gehen Suchgebiet, Typfilter, Wunschtext und bereits vorhandene POI-Namen — keine Teilnehmerdaten, keine Kennungen. Es gibt weiterhin **keine Upload- oder Bild-Auslieferungsroute** (`IMAGE_DIR` wird in `lib/` und `app/` nirgends gelesen), also auch keine erratbaren Beleg-URLs. Die in `delivery/security.md` geforderten Löschpflichten für Standortdaten sind mangels Standort-Funktion noch nicht relevant.

Der Bericht wird als Datei unter `delivery/security/` abgelegt.
